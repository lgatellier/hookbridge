[tool.poetry]
name = "webhook-router"
version = "0.1.0"
description = "A configurable router for JSON webhooks"
authors = ["Léo GATELLIER <github@leogatellier.fr>"]

[tool.poetry.dependencies]
python = "^3.9"
fastapi = "^0.70.0"
uvicorn = "^0.15.0"
dependency-injector = "^4.37.0"
PyYAML = "^6.0"
coloredlogs = "^15.0.1"
jsonpath-ng = "^1.5.3"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
tox = "^3.24.4"
black = "^21.10b0"
flake8 = "^4.0.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
